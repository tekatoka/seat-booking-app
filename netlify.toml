[build]
  command = "yarn build"
  functions = "functions"

# this is actually a Rewrite
# totally optional, but makes up for
# a much better UX
[[redirects]]
  from = "/"
  to = "/.netlify/functions/api"
  status = 200